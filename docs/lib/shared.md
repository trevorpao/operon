# Shared Runtime Helpers

> Stage 1 會補上每個 helper 的簽名、用途與 SSR 注意事項。

## Overview
- 描述 `withBrowser`、`withDocument`、`ensureBrowser`、`toElements` 等工具的角色。

## Pending Tasks
- [ ] 梳理 exports 與對應的使用範例。
- [ ] 加入 teardown/fallback 模板。
