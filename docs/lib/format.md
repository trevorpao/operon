# Format Plugin

> Stage 2 將整理 `createFormatHelper`, `registerTemplateHelpers`, `formatNum`, `thumbnail` 等工具，說明依賴注入與 Handlebars 整合界線。

## Overview
- 介紹 format plugin 初始化流程與 app/Handlebars 的掛載方式。

## Pending Tasks
- [ ] 梳理 helper 清單與輸入輸出。
- [ ] 補充 Gee/Handlebars 依賴的 fallback 策略。
