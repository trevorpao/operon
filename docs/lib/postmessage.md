# PostMessage Helpers

> Stage 2 會補上 `postMessage`, `receiveMessage`, `createMessageValidator`, `createMessageListener`, `requestResponse` 的範例與 schema 指南。

## Overview
- 敘述跨視窗通訊流程、payload 驗證與 timeout/fallback 設計。

## Pending Tasks
- [ ] 製作 iframe 互動流程圖。
- [ ] 補充 validator/request 範例與錯誤處理建議。
